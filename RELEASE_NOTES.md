# v1.0.0 · 初次发布 🎉

一个直观的 Excel 跨表查询与核对工具——把 VLOOKUP、差异对比、分组聚合变成三下五除二的事。

## 📥 直接下载使用

| 版本 | 下载 | 当前构建大小 | 适用 |
|---|---|---|---|
| 🖥 **桌面版（推荐）** | `跨表核对.zip` | 解压约 230MB / zip 约 103MB | 日常使用，启动快，原生 Qt 界面 |
| 🌐 Web 版 | `跨表核对Web.zip` | 解压约 480MB / zip 约 182MB | 浏览器内操作，Streamlit 实现 |

**无需安装 Python 或任何依赖**，Windows 双击 exe 即可运行。

### 使用步骤

1. 在 GitHub Releases 下载对应 zip
2. 解压到任意目录
3. 双击其中的 `跨表核对.exe`（或 `跨表核对Web.exe`）
4. 点击工具栏 `ℹ 使用说明` 查看内置帮助

> ⚠️ `_internal` 文件夹必须与 exe 同目录，不能单独移动 exe。

## ✨ 功能亮点

### ① 自定义分组聚合
选任意 Excel → 选主索引列 → 为其他每列独立选聚合方式（`sum` / `max` / `min` / `first` / `last` / `count` / `concat去重` / `mean` / 跳过）。每个选项都带详细中文解释。智能默认值按列名/类型自动推荐。

### ② 跨表查询及核对
双模式：
- **核对** — 以 A 表为基表，比对 B/C/D... 多个参考表的多组『目标数据』，输出完整差异报告
- **查询** — 从参考表查到数据回填到 A 表副本（不改原文件），附带『原值 / 来源数据值 / 来源记录数 / 匹配参考表 / 匹配状态』辅助列

目标数据编号以 A 表为准，参考表之间不能重复占用同一编号。参考表支持多 Sheet 合并；各表都支持 pandas query 过滤表达式（如 `金额>1000`）、键列规范化（自动处理工号前导零）、差额容差。

### ③ 带运算核对指定列
行内公式核对：`实际值列 = X 列 [+ - × ÷] Y 列`。可指定『携带列』便于定位差异行。

## 🔒 隐私
所有处理都在本地完成，不上传任何数据到任何服务器。Web 版只监听 localhost。

## 📝 许可证
MIT License — 可自由使用、修改、分发。

---

**完整文档、源码、开发说明请见项目主页：**
https://github.com/jiushinaozhewan/shujuduibi
